/**
 * Legacy client AI helpers.
 * Live chat now runs through /api/chat (server-side keys + tools).
 * Kept for shared prompt text used by documentation and older imports.
 */
export { SYSTEM_PROMPT, MODEL_CONFIG } from './ai-config'

export type { CineBotUIMessage as ChatMessage } from './chat-types'
